```ts
// README
/*
Quickstart:
- pnpm install
- pnpm dev
- pnpm build

Quality:
- pnpm lint
- pnpm typecheck
- pnpm test
- pnpm test:coverage

Assumptions:
- BazarBin backend API is reachable via VITE_BAZARBIN_HOST (defaults to http://localhost:5275).
- Sample CSV files exist relative to the frontend when referencing import examples.

Limitations:
- Frontend expects API contract described in src/lib/imports.ts; deviations require code updates.
- No offline fallback; importing requires a live backend.
*/
```
